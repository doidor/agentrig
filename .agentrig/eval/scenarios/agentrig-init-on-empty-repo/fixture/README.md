# Empty repo

This is a tiny synthetic repository used by the `agentrig-init-on-empty-repo`
scenario. It exists to exercise `agentrig init` end-to-end without any of the
target-repo nuances that the AgentRig developer's own workspace would bring.

Nothing else lives here.
